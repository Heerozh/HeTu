# todo 测试连接，包括flood检测等，特别是future不应该遇到flood检测

# async def test_connect_with_system_executor(comp_mgr):
#     from hetu.system import SystemExecutor
#     executor = SystemExecutor('ssw', comp_mgr)
#     await executor.initialize("")
